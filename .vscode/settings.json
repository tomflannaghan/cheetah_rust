{
    "sqltools.useNodeRuntime": true,
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "Processed",
            "database": "${workspaceFolder:cheetah_rust}/data/processed.db"
        }
    ]
}